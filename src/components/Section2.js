import React, { PropTypes } from 'react';
import { Container, Row, Col } from 'react-bootstrap';

const Section2 = () => {
    return (
        <Container className='tmp-section-background' style={{}} fluid>
        	<Container className='tmp-section'>
        		<h1>Section 2</h1>
        	</Container>
        </Container>
    );
};

export default Section2;
